# ruff 基线配置（2026-08-04 建立）
# 目标：对新增代码严格检查，对历史代码只保留安全规则（E4/E7/E9/F/I）。
# 说明：本项目历史代码大量使用 Optional 等旧风格（UP 规则会全量报错），
# 因此基线只启用安全子集；后续想收紧规则时再逐步放开。

line-length = 120
target-version = "py310"

[lint]
select = ["E4", "E7", "E9", "F", "I"]
