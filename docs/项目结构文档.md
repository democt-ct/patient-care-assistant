# 患者照护助手 — 项目结构文档

> 更新日期：2026-08-03

---

## 目录

1. [项目概览](#1-项目概览)
2. [根目录结构](#2-根目录结构)
3. [app/ 核心应用](#3-app-核心应用)
4. [数据模型层 (app/models/)](#4-数据模型层-appmodels)
5. [API 路由层 (app/api/)](#5-api-路由层-appapi)
6. [MCP 工具层 (app/mcp/)](#6-mcp-工具层-appmcp)
7. [业务服务层 (app/services/)](#7-业务服务层-appservices)
8. [Schema 层 (app/schemas/)](#8-schema-层-appschemas)
9. [前端测试页 (app/static/)](#9-前端测试页-appstatic)
10. [数据存储 (data/)](#10-数据存储-data)
11. [启动与脚本](#11-启动与脚本)
12. [文档目录 (docs/)](#12-文档目录-docs)
13. [Reasonix 配置 (.reasonix/)](#13-reasonix-配置-reasonix)

---

## 1. 项目概览

**项目名称**：Patient Care Assistant（患者照护助手）

**技术栈**：

| 层次        | 技术                                                      |
| ----------- | --------------------------------------------------------- |
| Web 框架    | FastAPI 0.103.2                                           |
| ASGI 服务器 | Uvicorn 0.24.0                                            |
| ORM         | SQLAlchemy 2.0.25                                         |
| 数据校验    | Pydantic 2.12.5                                           |
| 数据库      | PostgreSQL 15 + Redis 7                                   |
| 向量数据库  | ChromaDB（`data/chroma_knowledge/`）                    |
| 文本生成    | DeepSeek-V4-Flash（ModelScope API）                       |
| 图片理解    | ModelScope Qwen3.5-397B-A17B（远程）                      |
| 向量模型    | BAAI/bge-small-zh-v1.5（sentence-transformers）           |
| TTS         | Kokoro（本地）/ OpenAI Compatible TTS（远程）             |
| LLM 编排    | LangChain Core + LangChain OpenAI + LangChain HuggingFace |

**核心**

- 患者主档、病历、就诊记录 CRUD
- 自研模块化 MCP 工具层（6 个核心工具）
- 四层记忆系统：短期工作记忆 → 长期摘要记忆 → 知识记忆 → 事实记忆
- 混合 RAG 检索（向量 + 关键词 + 元数据 + 时效）
- 图文问答 + 语音播报
- Memory Debug 可视化

---

## 2. 根目录结构

```
项目根目录/
├── app/                          # ★ 核心应用包
│   ├── main.py                   #   FastAPI 入口（启动、路由注册、生命周期）
│   ├── api/                      #   API 路由层（含 evaluation_routes）
│   ├── config/                   #   配置层（production / local_settings / evaluation_cases）
│   ├── core/                     #   基础设施（数据库连接、KV 存储）
│   ├── mcp/                      #   MCP 工具服务器
│   ├── models/                   #   SQLAlchemy ORM 模型
│   ├── schemas/                  #   Pydantic 请求/响应 Schema
│   ├── services/                 #   业务服务层
│   ├── utils/                    #   通用工具（脱敏等）
│   └── static/                   #   静态文件（测试页 + 评估控制台）
│
├── data/                         # ★ 数据存储目录
│   └── chroma_knowledge/         #   ChromaDB 向量库
│
├── docs/                         # ★ 文档目录
│   ├── 项目结构文档.md            #   本文档
│   ├── patient_medical_information_agent_design.md  #   Agent 主线设计
│   ├── 执行计划.md                #   秋招 MVP 执行基线
│   ├── 秋招黄金场景.md            #   四个黄金场景（冻结基线）
│   ├── 秋招演示脚本.md            #   三分钟演示脚本
│   ├── 基线报告.md                #   指标基线（待实测回填）
│   ├── 医疗安全与知识治理.md      #   安全门禁、知识审核、过敏五层防护
│   ├── 问诊降级策略.md            #   模型不可用时的降级策略
│   ├── docker_deployment.md      #   Docker + PostgreSQL/Redis 部署与手动安装
│   ├── 面试准备-项目知识点.md      #   面试知识点 + 高频追问速答
│   └── 照护计划MVP.md             #   照护计划扩展模块设计
│
├── scripts/                      # 工具脚本（批量导入、质量评估、种子数据、cli_query）
├── tests/                        # pytest 测试（含 test_evaluation / test_risk_signals）
├── image/                        # 测试图片
│
├── requirements.txt              # Python 核心依赖清单
├── requirements-optional.txt     # 可选重依赖（本地向量嵌入 / Kokoro TTS，懒加载）
├── ruff.toml                     # ruff lint 基线配置
├── .gitignore                    # Git 忽略规则
├── reasonix.toml                 # Reasonix 配置
├── cloudflared.example.yml       # Cloudflare Tunnel 配置模板
├── Dockerfile                    # Docker 应用镜像
├── docker-compose.yml            # PostgreSQL + Redis 容器编排
├── rebuild_frontend.bat          # 前端构建脚本
│
├── start_dev.bat                 # 本地启动（127.0.0.1:8001）
├── start_tunnel.bat              # 内网穿透（Cloudflare 隧道，自动检测命名/快速）
│
├── .reasonix/                    # Reasonix Code 配置
│   ├── skills/                   #   自定义技能（planning-with-files + superpower）
│   └── templates/                #   计划模板
├── .vscode/                      # VS Code 工作区配置
├── .claude/                      # Claude Code 配置
└── .cloudflared/                 # Cloudflare 隧道运行时配置
```

---

## 3. app/ 核心应用

### 3.1 `app/main.py` — 应用入口

FastAPI 应用工厂，负责：

- 创建 `FastAPI` 实例（title="Patient Agent Data Service", version="0.3.1"）
- `startup` 事件：自动建表 + 预热知识检索器
- 注册六个路由模块：`router`（数据）、`memory_router`（记忆）、`mcp_router`（MCP/Agent）、`stream_router`（SSE 流式）、`evaluation_router`（质量评估）、`care_plan_router`（照护计划）
- 提供 `/health`、`/`、`/tester` 端点

### 3.2 `app/core/` — 基础设施

| 文件                | 职责                                                                       |
| ------------------- | -------------------------------------------------------------------------- |
| `database.py`     | SQLAlchemy 引擎创建（PostgreSQL）、Session 工厂、`get_db` 依赖注入生成器 |
| `redis_client.py` | Redis 客户端（单例模式，含 Mock 降级），会话缓存与短期工作记忆存取         |
| `logging.py`      | 结构化日志（支持 JSON / text 格式），请求日志中间件；日志携带 `trace_id` |
| `metrics.py`      | Prometheus HTTP、Agent、LLM 调用/耗时/token 指标；HTTP 路径按路由模板聚合 |
| `tracing.py`      | Agent span、错误/升级计数与可选 OTLP 导出；患者/会话标识哈希化 |

---

## 4. 数据模型层 (app/models/)

> 当前共 17 张 ORM 表：患者数据 3 张、记忆 8 张、照护 4 张、审计与评估 2 张。

所有模型通过 `app/models/__init__.py` 统一导出，在 `app/main.py` 中通过 `import app.models` 触发注册。

### 4.1 患者数据模型

| 模型              | 表名                | 说明                                                                      |
| ----------------- | ------------------- | ------------------------------------------------------------------------- |
| `Patient`       | `patients`        | 患者主档（姓名、性别、出生日期、手机号、过敏史、地址、医院 ID、活跃状态） |
| `MedicalRecord` | `medical_records` | 病历记录（诊断、ICD 编码、主诉、医嘱、检查结果，关联 patient_id）         |
| `VisitRecord`   | `visit_records`   | 就诊记录（就诊日期、科室、医生、就诊类型，关联 patient_id）               |

### 4.2 记忆系统模型

| 模型                           | 表名                               | 说明                                                             |
| ------------------------------ | ---------------------------------- | ---------------------------------------------------------------- |
| `MemoryConversationMessage`  | `memory_conversation_messages`   | 短期对话消息（按 patient_id + session_id 组织）                  |
| `MemorySessionBufferMessage` | `memory_session_buffer_messages` | 匿名会话缓冲消息（身份确认前暂存）                               |
| `MemoryBusinessProfile`      | `memory_business_profiles`       | 业务画像（从病历/就诊提取的医学摘要）                            |
| `MemoryConversationProfile`  | `memory_conversation_profiles`   | 对话画像（从对话中提取的沟通特征）                               |
| `MemoryUserProfile`          | `memory_user_profiles`           | 融合画像（业务 + 对话合并）                                      |
| `MemoryKeyEvent`             | `memory_key_events`              | 关键事件（有长期价值的知识点，支持 canonical_key 幂等合并）      |
| `MemoryPreference`           | `memory_preferences`             | 偏好设置（回答风格、术语级别、风险提醒强度等）                   |
| `MemoryKnowledgeChunk`       | `memory_knowledge_chunks`        | 知识块（用于 RAG 检索，含 effective_from / expires_at 版本控制） |

---

## 5. API 路由层 (app/api/)

### 5.1 `routes.py` — 患者数据接口

- **前缀**：`/api/v1`
- **标签**：`patient-data`

| 方法       | 路径                                              | 说明             |
| ---------- | ------------------------------------------------- | ---------------- |
| `POST`   | `/api/v1/patients`                              | 创建患者         |
| `GET`    | `/api/v1/patients`                              | 查询患者列表     |
| `GET`    | `/api/v1/patients/{patient_id}`                 | 查询单个患者     |
| `DELETE` | `/api/v1/patients/{patient_id}`                 | 删除患者         |
| `POST`   | `/api/v1/patients/{patient_id}/medical-records` | 创建病历         |
| `GET`    | `/api/v1/patients/{patient_id}/medical-records` | 查询病历列表     |
| `POST`   | `/api/v1/patients/{patient_id}/visits`          | 创建就诊记录     |
| `GET`    | `/api/v1/patients/{patient_id}/visits`          | 查询就诊记录列表 |

### 5.2 `memory_routes.py` — 记忆系统接口

- **前缀**：`/api/v1/memory`
- **标签**：`memory`

核心端点：

| 方法       | 路径                               | 说明                            |
| ---------- | ---------------------------------- | ------------------------------- |
| `GET`    | `/profile`                       | 获取患者完整记忆画像            |
| `GET`    | `/conversations/messages`        | 查询对话消息                    |
| `DELETE` | `/conversations/messages`        | 清除长期记忆                    |
| `GET`    | `/conversations/sessions`        | 查询会话列表                    |
| `POST`   | `/conversations/promote-session` | 匿名会话升级（绑到 patient_id） |
| `POST`   | `/knowledge-chunks`              | 创建知识块                      |
| `POST`   | `/knowledge-chunks/retrieve`     | 知识检索（RAG）                 |
| `POST`   | `/extract`                       | 触发记忆抽取                    |
| `POST`   | `/preferences`                   | 更新偏好                        |
| `GET`    | `/preferences`                   | 查询偏好                        |

### 5.3 `mcp_routes.py` — MCP 工具与 Agent 接口

- **前缀**：`/api/v1/mcp`
- **标签**：`mcp-server`

| 方法     | 路径                        | 说明             |
| -------- | --------------------------- | ---------------- |
| `GET`  | `/health`                 | MCP 服务健康检查 |
| `GET`  | `/tools`                  | 列出所有注册工具 |
| `POST` | `/call`                   | 调用指定工具     |
| `POST` | `/auth/issue-token`       | 签发身份 token   |
| `POST` | `/agent/query`            | 智能问答（文本） |
| `POST` | `/agent/query-with-image` | 智能问答（图文） |
| `POST` | `/agent/speech`           | 文字转语音       |

### 5.4 `stream_routes.py` — SSE 流式接口

- **前缀**：`/api/v1/stream`
- **标签**：`stream`

提供 Server-Sent Events 流式问答（增量输出 LLM 回答）。同步 Agent 调用在线程中执行，并受 `STREAM_AGENT_TIMEOUT_SECONDS` 控制；模型超时或异常时仍返回安全降级回答和 `done` 事件。

### 5.5 `evaluation_routes.py` — 质量评估接口

- **前缀**：`/api/v1/evaluation`
- **标签**：`evaluation`

| 方法    | 路径       | 说明                                               |
| ------- | ---------- | -------------------------------------------------- |
| `GET` | `/cases` | 返回完整评估用例集（单一数据源，47 条分层用例 + 聚合统计） |

---

## 6. MCP 工具层 (app/mcp/)

### 6.1 核心模块

| 文件              | 职责                                                                                                     |
| ----------------- | -------------------------------------------------------------------------------------------------------- |
| `server.py`     | `ModularMCPServer` — 工具注册、列表、调用分发、身份解析                                               |
| `auth.py`       | HMAC-SHA256 身份 token 签发与校验                                                                        |
| `config.py`     | 从`app/config/local_settings.py` 加载配置，合并默认值（旧路径 `app/mcp/local_settings.py` 向后兼容） |
| `llm_router.py` | LLM 编排：意图识别 → 计划生成 → 答案生成 → 记忆持久化                                                 |
| `llm_router/pipeline.py` | Agent Graph 编排：safety → task_route → retrieval → evidence_check → citation_validate → output_assemble |
| `llm_router/output_contract.py` | 统一五段输出装配（answer / evidence_summary / risk_level / next_action / agent_trajectory） |
| `schemas.py`    | MCP 数据模型（`MCPToolDefinition`、`MCPToolCallResponse`）                                           |
| `speech.py`     | TTS 双引擎（OpenAI Compatible / Kokoro），含降级链                                                       |
| `vision.py`     | 视觉 LLM 调用，图片 Base64 → 文字摘要，含模型回退                                                       |

> 本地配置文件 `local_settings.py` / `local_settings.example.py` 已迁移到 `app/config/`，详见 [§ 配置层](#配置层-appconfig)。

### 6.2 注册的 MCP 工具

| 工具名                   | 功能                 | 身份校验                 |
| ------------------------ | -------------------- | ------------------------ |
| `issue_identity_token` | 签发测试用身份 token | 仅测试环境               |
| `verify_identity`      | 校验 token 有效性    | 返回身份上下文           |
| `get_medical_records`  | 查询患者病历         | patient_id 或 auth_token |
| `get_visit_records`    | 查询就诊记录         | patient_id 或 auth_token |
| `get_patient_profile`  | 聚合患者完整信息     | patient_id 或 auth_token |
| `get_my_care_plans`    | 查询已发布照护计划   | patient_id 或 auth_token |

### 6.3 LLM Router 问答流程

```
用户输入 → 确定性安全门禁(emergency/clinician_review/allow)
    → 任务路由(RetrievalRoute) → 结构化检索(EvidencePack)
    → 证据检查(充分/缺失/冲突/高风险，补检索最多一次)
    → 生成(执行适配器) → 引用校验 → 统一五段输出 + 记忆持久化
```

---

## 7. 业务服务层 (app/services/)

| 文件                             | 职责                                                                                  |
| -------------------------------- | ------------------------------------------------------------------------------------- |
| `patient_service.py`             | 患者 CRUD、病历/就诊记录查询、聚合画像查询                                            |
| `memory_extraction_service.py`   | 记忆抽取：短期消息写入、长期记忆提取、三层画像生成、关键事件合并、知识块管理          |
| `memory_preference_service.py`   | 偏好设置 CRUD                                                                         |
| `knowledge_retrieval.py`         | 混合 RAG 检索：向量召回（ChromaDB）+ 关键词补召回 + 元数据过滤 + 时效加权 + 可选重排序 |
| `retrieval_router.py`            | 确定性任务分类与检索路由：任务、允许数据源、禁止动作、补检索上限                  |
| `agentic_retrieval.py`           | 按路由把结构化事实/时间线/审核知识组装成 EvidencePack，补检索合并                |
| `evidence_policy.py`             | 证据充分性 / 冲突 / 高风险判定，决策优先级 emergency > high_risk > conflict > missing > sufficient |
| `citation_validator.py`          | 确定性引用校验：回答中的药物实体 / 日期 / 剂量与 EvidenceItem 对应关系           |
| `evaluation_service.py`         | 服务端评估评分（三维加权）、运行记录持久化与趋势聚合                                  |
| `care_plan_service.py`           | 照护计划生成、确认、任务状态机、协作工单管理                                          |
| `clinical_knowledge_governance.py` | 临床知识治理：准入校验、审核流程、来源白名单验证                                    |
| `demo_seed.py`                   | 演示种子数据生成                                                                      |

---

## 8. Schema 层 (app/schemas/)

Pydantic v2 模型，定义 API 请求/响应的数据结构：

| 文件                     | 内容                                                            |
| ------------------------ | --------------------------------------------------------------- |
| `patient.py`           | `PatientCreate`、`PatientRead`                              |
| `medical_record.py`    | `MedicalRecordCreate`、`MedicalRecordRead`                  |
| `visit_record.py`      | `VisitRecordCreate`、`VisitRecordRead`                      |
| `profile.py`           | `PatientProfileRead`（聚合响应）                              |
| `memory_extraction.py` | 记忆抽取相关 Schema（消息、会话、画像、关键事件、知识块检索等） |
| `memory_preference.py` | `MemoryPreferenceRead`、`MemoryPreferenceUpsert`            |
| `care_plan.py`         | 照护计划/任务/事件/协作单 Schema                             |
| `retrieval.py`         | Agentic RAG 契约：`TaskType`、`RetrievalRoute`、`EvidenceItem`、`EvidencePack`、`EvidenceCheck`、`AgentOutputContract` |

---

## 9. 前端 (app/static/ + frontend/)

### 9.1 React 前端（`frontend/`）

新版正式前端，React 18 + TypeScript + Vite，支持：

- 聊天界面（通用聊天 / 记忆聊天）
- 图文问答 + 语音播报
- Memory Debug（中文卡片 + 原始 JSON）
- 执行步骤摘要（默认收起）
- 质量评估控制台（`/evaluate`）

### 9.2 旧版前端（`app/static/`）

保留兼容的旧版测试页面和独立 CSS/JS 模块。

| 文件              | 说明                                                                                                                                              |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `index.html`    | 新版模块化前端主页，支持 Query / 通用聊天 / 记忆聊天 / 图文问答 / 语音播报 / Memory Debug                                                         |
| `tester.html`   | 旧版单文件测试页面（功能一致，逐步迁移到 index.html）                                                                                             |
| `evaluate.html` | 质量评估控制台（HTML 骨架，引用`css/evaluate.css` + `js/evaluate.js`）                                                                        |
| `css/`          | 样式表模块（`base.css`、`chat.css`、`components.css`、`layout.css`、`pages.css`、`variables.css`、`evaluate.css`）                  |
| `js/`           | JavaScript 模块（`api.js`、`app.js`、`chat.js`、`memory.js`、`patient.js`、`speech.js`、`state.js`、`utils.js`、`evaluate.js`） |

Memory Debug 可查看：工作记忆、患者事实记忆、长期摘要记忆、知识记忆、最终合并上下文。

质量评估控制台（`/evaluate`）：47 条分层评估用例（18 开发集 / 29 独立测试集）从 `GET /api/v1/evaluation/cases` 动态加载（单一数据源在 `app/config/evaluation_cases.py`），支持按前缀与开发集/独立测试集筛选、三维加权评分、Agent 路由/证据/安全指标、RAGAS LLM-as-Judge、意图混淆矩阵、CSV 导出。评分与持久化由服务端 `POST /api/v1/evaluation/runs` 统一完成，运行记录包含用例与评分版本、可选模型/提示词/知识库版本及 `trace_id`，但不持久化原始回答。高风险用例使用 `safety_policy` 校验必要安全提示与危险建议模式，因此“不要自行停药”等安全提醒不会被误报为违规。

事实查询直答：当用户询问已记录的诊断、既往用药、手术、接诊医生、复诊安排、过敏史或紧急联系人时，Agent 直接读取结构化病历、就诊记录或患者主档并生成固定格式回答，不调用 LLM 改写；需要跨记录综合、解释或临床判断的问题仍由 Agent 链路处理。`scripts/seed_patients.py` 可重复运行，按患者编码及记录标识补齐缺失的演示数据，而不会覆盖已有记录。

问答执行步骤：React 聊天页会在每条完成的助手回答下保留默认收起的“执行步骤”卡片，展示身份校验、上下文加载、意图识别、信息查询与回答生成等用户可读阶段。该摘要不展示原始病历、工具参数或模型内部推理；后端仍保留安全的结构化日志、`trace_id`、OTel 与 `/metrics` 能力，便于在需要时接入企业已有的监控平台。

---

## 配置层 (app/config/)

| 文件                           | 职责                                                                                                                   |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| `production.py`              | 生产环境配置（数据库、Redis、CORS 等基础设施参数）                                                                     |
| `local_settings.py`          | 本地实际配置（含真实 API 密钥，被`.gitignore` 忽略，不入 Git）                                                       |
| `local_settings.example.py`  | 本地配置模板（复制为`local_settings.py` 后填入密钥）                                                                 |
| `evaluation_cases.py`        | 质量评估用例的**单一数据源**，被 HTTP API、命令行运行器、前端控制台共享                                                |
| `faq_knowledge.py`           | FAQ 知识库配置                                                                                                         |
| `medical_source_registry.py` | 医学知识源白名单注册表（域名/来源类型/发布权限）                                                                       |

**配置优先级**（`app/mcp/config.py:_get_setting`）：
`app/config/local_settings.py` > 环境变量 > 代码默认值

---

## 10. 数据存储

```
PostgreSQL 15（主数据库）              # 患者、病历、记忆等全部结构化数据
Redis 7（会话缓存）                     # 短期工作记忆 + Session 缓存
data/
└── chroma_knowledge/                  # ChromaDB 向量库（知识块嵌入）
    └── {uuid}/                        #   Chroma Collection 数据
        ├── data_level0.bin
        ├── header.bin
        └── length.bin
```

---

## 11. 启动与脚本

### 11.1 启动脚本

| 文件                 | 说明                                                                    |
| -------------------- | ----------------------------------------------------------------------- |
| `start_dev.bat`    | 本地启动，监听`127.0.0.1:8001`，自动处理 Docker + PG + uvicorn        |
| `start_tunnel.bat` | 一键内网穿透，自动检测命名隧道/快速隧道，后台启动后端 + Cloudflare 隧道 |

### 11.2 工具脚本（`scripts/`）

| 文件                  | 说明                                                                       |
| --------------------- | -------------------------------------------------------------------------- |
| `cli_query.py`      | 命令行测试客户端，支持`query` / `query-with-image` / `speech` 子命令 |
| `bulk_import.py`    | 批量导入 CSV/JSON 患者数据，支持去重、更新、自动知识切片                   |
| `seed_patients.py`  | 种子数据生成（测试用患者、病历、就诊记录）                                 |
| `run_evaluation.py` | 质量评估命令行运行器（从`tests/test_evaluation.py` 加载用例）            |
| `init_postgres.py`  | PostgreSQL 数据库初始化脚本                                                |
| `setup_services.py` | 服务环境配置脚本                                                           |

---

## 12. 文档目录 (docs/)

| 文档 | 说明 |
|------|------|
| `项目结构文档.md` | **本文档** — 项目完整结构索引 |
| `面试准备-项目知识点.md` | 面试知识点（四层记忆、RAG、主线问答、高频追问速答） |
| `医疗安全与知识治理.md` | 安全门禁、知识审核、来源白名单、过敏五层防护 |
| `patient_medical_information_agent_design.md` | 患者医疗信息核验与就医导航 Agent 的 MVP 架构、证据边界与安全策略 |
| `执行计划.md` | 面向秋招交付的 Agent MVP 阶段计划、验收指标与简历材料 |
| `秋招黄金场景.md` | 四个黄金场景的冻结数据、固定问题与输出边界 |
| `秋招演示脚本.md` | 三分钟演示脚本（事实查询 → 危险拦截 → 冲突提示） |
| `基线报告.md` | 当前版本基线快照与待实测指标口径 |
| `照护计划MVP.md` | 照护计划模块设计文档 |
| `docker_deployment.md` | Docker 部署 + PostgreSQL/Redis 手动安装指南 |
| `问诊降级策略.md` | 模型不可用时的降级策略 |

---

## 13. Reasonix 配置 (.reasonix/)

Reasonix Code 的项目配置目录，存放自定义技能、计划模板和运行时数据。

| 路径                               | 说明                                                                                                   |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------ |
| （已移除）                         | sync-docs skill 已迁移至全局安装（`%APPDATA%/reasonix/skills/sync-docs/SKILL.md`），不再存于项目目录 |
| `skills/planning-with-files.md`  | 文件化规划 skill                                                                                       |
| `skills/superpower.md`           | Superpower 基础 skill                                                                                  |
| `templates/planning-with-files/` | 规划模板（findings / progress / task_plan）                                                            |
| `truncated-results/`             | 截断命令结果缓存                                                                                       |
| `desktop-topic-*.json`           | 桌面主题元数据                                                                                         |

---

## 附录：关键架构模式

### A. 记忆金字塔

```
        查询频率高 / 容量小
  ↑ 工作记忆（内存，最近 5 轮）
  │ 会话缓冲（匿名，瞬态）
  │ 短期消息（DB，有限保留）
  │ 关键事件（DB，长期，去重合并）
  │ 画像（DB，持续更新摘要）
  ↓ 偏好（DB，几乎不变）
        查询频率低 / 容量大
```

### B. 规则优先 + LLM 兜底

在意图识别、实体提取、身份解析等环节：先尝试确定性规则（快、可解释、零成本），失败时再调用 LLM。

### C. 降级链

嵌入层、视觉层、语音层均实现多级降级：首选方案 → 次选方案 → 本地兜底方案。

### D. 匿名 → 实名过渡

```
匿名会话缓冲（不关联 patient_id）
  → promote（身份确认后迁移到正式表）
  → 正式短期消息（关联 patient_id）
```

### E. 数据流水线

```
原始输入 → 身份解析 → 记忆构建 → 上下文组装 → 工具执行 → 答案生成
```
